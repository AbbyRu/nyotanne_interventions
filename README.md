# Nyota Nne Interventions Website

Standalone static website. Each HTML page includes its own header, navigation, footer and mobile-menu script. All pages share `styles.css` and `logo-new.jpeg`.

## Before publishing
- Replace the team-page placeholders with approved names, roles, biographies, qualifications and images.
- Replace `YOUR_FORM_ID` in `contact.html` with the correct Formspree form ID.
- Review all service descriptions with the relevant professionals.
